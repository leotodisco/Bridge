/**
 * Contiene tutte le classi, i servizi e i componenti principali dell'applicazione backend.
 * <p>
 * Questo pacchetto implementa la logica di business e la gestione dei dati per l'applicazione,
 * includendo:
 * - Modelli (Entity) che definiscono la struttura dei dati
 * - DAO (Data Access Objects) per l'interazione con il database
 * - DTO (Data Transfer Objects) per il passaggio delle informazioni
 * - Servizi che implementano la logica di business
 * - Interfacce per la definizione dei servizi
 * - Controller per il mapping dei servizi e la gestione delle richieste HTTP
 * - Componenti di sicurezza, come i filtri di autenticazione e la gestione dei token
 * - Configurazioni per la gestione di CORS, della sicurezza e di altre impostazioni applicative
 * </p>
 * <p>
 * In questo pacchetto sono inclusi anche gli endpoint pubblici (come login, registrazione, ecc.),
 * che sono accessibili senza autenticazione, e le configurazioni per la gestione degli errori.
 * </p>
 */

package com.project.bridgebackend;
